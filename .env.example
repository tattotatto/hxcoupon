# Server
SERVER_MODE=release

# 宿主机 MySQL（数据库需提前创建好）
DB_USER=root
DB_PASSWORD=replace-with-your-mysql-password
DB_NAME=hxcoupon

# Redis（容器内运行）
REDIS_PASSWORD=

# JWT
JWT_SECRET=replace-with-random-string-at-least-32-chars
